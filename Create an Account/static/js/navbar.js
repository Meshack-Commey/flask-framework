const createNav = () => {
  let nav = document.querySelector('#navbar');
  
  nav.innerHTML = "";
}

createNav();

//setTimeout(createNav, 1000);

//setInterval(creatrNav, 2000);

//clearInterval(createNav, 3000);

